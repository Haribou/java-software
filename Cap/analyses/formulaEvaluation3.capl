# Formula evaluator using JSON input.

debug: off;

constants:

;

variables:

;

updateRules:

;

outputRules:

# Format of output rules:
# label: condition : expression [else expression]

dataVolume: @count > 100: "lots of data" else "not much data",
hasKey: @containsKeys("value10", "value11") | @["value1"] < 0: "ok" else "sorry!",
hasValues: @containsValues(26) | !@containsValues(-219): "bingo" else "sorry!";