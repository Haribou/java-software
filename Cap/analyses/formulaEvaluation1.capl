# Formula evaluator using JSON input.

debug: off;

constants:

;

variables:

;

updateRules:

;

outputRules:

# Format of output rules:
# label:: expression

formulaValue:: @["value1"] + @["value2"] * @["value3"] / (@["value4"] - @["value5"]) + log @["value6"] - (@["value7"] / 10)^2;